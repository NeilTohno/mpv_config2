Download ffmpeg full package and extract to here.
You can add this directory to the system environment "path",
And copy the two batch files to any directory for execution.

下载 ffmpeg 完整包并解压缩到此处。
你可以将此目录添加到系统环境路径 "path" 中，
并且可以将这两个批处理文件复制到任何目录来执行。
